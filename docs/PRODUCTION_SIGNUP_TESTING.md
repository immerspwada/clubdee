# คู่มือทดสอบการสมัครสมาชิกใน Production

## ปัญหา: ติด Rate Limit จากการทดสอบก่อนหน้า

เมื่อทดสอบการสมัครหลายครั้งจาก IP เดียวกัน Supabase จะบล็อกการสมัครเป็นเวลา 1-2 ชั่วโมง

---

## ✅ วิธีที่ 1: ใช้ Admin API ทดสอบ (แนะนำ)

**ข้อดี:**
- ✅ ไม่ถูก rate limit
- ✅ ทดสอบได้ทันที
- ✅ ทดสอบ end-to-end ทั้งหมด (auth + profile + athlete)
- ✅ สามารถ login ด้วยบัญชีที่สร้างได้

### วิธีใช้:

```bash
cd sports-club-management

# รันสคริปต์ทดสอบ
node scripts/test-production-signup.js
```

### ผลลัพธ์ที่คาดหวัง:

```
🧪 ทดสอบการสมัครสมาชิกใน Production

📝 ข้อมูลทดสอบ:
   Email: test-prod-1732567890123@example.com
   Password: TestPassword123!
   Name: Test User 1732567890123

1️⃣ สร้าง Auth User...
✅ สร้าง Auth User สำเร็จ
   User ID: abc123...

2️⃣ ตรวจสอบ Clubs...
✅ พบ Club: ชมรมกีฬา
   Club ID: xyz789...

3️⃣ สร้าง Profile...
✅ สร้าง Profile สำเร็จ

4️⃣ สร้าง Athlete Record...
✅ สร้าง Athlete Record สำเร็จ

5️⃣ ทดสอบ Login...
✅ Login สำเร็จ
   Session: มี

6️⃣ ทดสอบดึงข้อมูล Profile...
✅ ดึงข้อมูล Profile สำเร็จ
   Role: athlete
   Status: active

═══════════════════════════════════════
✅ ทดสอบสำเร็จทั้งหมด!
═══════════════════════════════════════

📋 ข้อมูลบัญชีทดสอบ:
   Email: test-prod-1732567890123@example.com
   Password: TestPassword123!
   User ID: abc123...
   Club ID: xyz789...

💡 คุณสามารถ login ด้วยบัญชีนี้ได้ที่ production URL
```

### ทดสอบ Login ด้วยบัญชีที่สร้าง:

1. เปิด production URL
2. ไปที่หน้า login
3. ใช้ email และ password จากผลลัพธ์
4. ตรวจสอบว่า login สำเร็จและเข้า dashboard ได้

---

## ✅ วิธีที่ 2: ทดสอบจาก IP อื่น

### Option A: ใช้ Mobile Hotspot

```
1. เปิด hotspot จากมือถือ
2. เชื่อมต่อคอมพิวเตอร์กับ hotspot
3. เปิด production URL
4. ทดสอบสมัครสมาชิกผ่าน UI ปกติ
```

**ข้อดี:**
- ทดสอบ UI flow จริงๆ
- ทดสอบ UX ทั้งหมด
- ไม่ถูก rate limit (IP ใหม่)

**ข้อเสีย:**
- ต้องมีมือถือที่เปิด hotspot ได้
- ใช้ mobile data

### Option B: ใช้ VPN

```
1. เปิด VPN (เช่น ProtonVPN, NordVPN)
2. เปลี่ยน location
3. เปิด production URL
4. ทดสอบสมัครสมาชิก
```

**ข้อดี:**
- ทดสอบ UI flow จริงๆ
- สามารถเปลี่ยน IP ได้หลายครั้ง

**ข้อเสีย:**
- ต้องมี VPN subscription
- อาจช้ากว่าปกติ

### Option C: ใช้ Incognito + Mobile Data

```
1. เปิด browser แบบ Incognito/Private
2. ปิด WiFi, เปิด mobile data (ถ้าเป็น laptop ที่มี SIM)
3. เปิด production URL
4. ทดสอบสมัครสมาชิก
```

### Option D: ขอเพื่อนช่วยทดสอบ

```
1. ส่ง production URL ให้เพื่อน
2. ให้เพื่อนลองสมัครสมาชิก
3. ตรวจสอบผลลัพธ์
```

**ข้อดี:**
- ทดสอบ real user experience
- ได้ feedback จริงๆ

---

## ✅ วิธีที่ 3: รอให้ Rate Limit หมดอายุ

```bash
# ตรวจสอบว่าครั้งสุดท้ายที่สมัครเมื่อไหร่
cd sports-club-management
./scripts/run-sql-via-api.sh scripts/check-rate-limit-stats.sql
```

**ผลลัพธ์:**
```sql
-- ถ้าเห็นว่าการสมัครครั้งสุดท้ายเกิน 1 ชั่วโมงแล้ว
-- แสดงว่า rate limit น่าจะหมดอายุแล้ว

time_ago: 01:15:23  -- เกิน 1 ชั่วโมงแล้ว = ปลอดภัย
```

**เมื่อ rate limit หมดอายุ:**
1. เปิด production URL
2. ทดสอบสมัครสมาชิกปกติ
3. ควรสำเร็จ (ถ้าไม่มีปัญหาอื่น)

---

## 🔍 ตรวจสอบว่าการสมัครทำงานปกติ

### Checklist:

#### ✅ 1. Auth User ถูกสร้าง
```bash
# ตรวจสอบใน Supabase Dashboard
# Authentication → Users
# หรือรัน SQL:
SELECT id, email, created_at 
FROM auth.users 
ORDER BY created_at DESC 
LIMIT 5;
```

#### ✅ 2. Profile ถูกสร้าง
```bash
SELECT id, email, full_name, role, membership_status
FROM profiles
WHERE email = 'test@example.com';
```

#### ✅ 3. Athlete Record ถูกสร้าง
```bash
SELECT user_id, club_id, date_of_birth
FROM athletes
WHERE user_id = 'user-id-here';
```

#### ✅ 4. Login ได้
```
1. ไปที่หน้า login
2. ใส่ email และ password
3. ควร redirect ไป /dashboard/athlete
```

#### ✅ 5. Dashboard แสดงข้อมูลถูกต้อง
```
- แสดงชื่อผู้ใช้
- แสดง club
- แสดงสถิติ (ถ้ามี)
```

---

## 🐛 Troubleshooting

### ปัญหา: ยังติด Rate Limit

**สาเหตุ:**
- Rate limit ยังไม่หมดอายุ (ต้องรอ 1-2 ชั่วโมง)
- ใช้ IP เดียวกัน

**วิธีแก้:**
1. ใช้วิธีที่ 1 (Admin API) แทน
2. หรือเปลี่ยน IP (mobile hotspot/VPN)

### ปัญหา: สร้าง User สำเร็จแต่ไม่มี Profile

**สาเหตุ:**
- Database trigger ไม่ทำงาน
- RLS policy บล็อก

**วิธีแก้:**
```bash
# ตรวจสอบ trigger
./scripts/run-sql-via-api.sh scripts/check-schema.sql

# สร้าง profile manually
./scripts/run-sql-via-api.sh scripts/create-profiles-for-users.sql
```

### ปัญหา: Login ไม่ได้

**สาเหตุ:**
- Email ยังไม่ verified
- Password ไม่ถูกต้อง

**วิธีแก้:**
```bash
# ตรวจสอบ user status
SELECT id, email, email_confirmed_at, created_at
FROM auth.users
WHERE email = 'test@example.com';

# ถ้า email_confirmed_at เป็น NULL
# ใช้ Admin API confirm email:
node scripts/confirm-user-email.js test@example.com
```

### ปัญหา: ไม่มี Club ในระบบ

**สาเหตุ:**
- ยังไม่ได้สร้าง club

**วิธีแก้:**
```bash
# สร้าง test data (รวม club)
./scripts/run-sql-via-api.sh scripts/03-setup-test-data.sql
```

---

## 📊 Monitoring

### ตรวจสอบสถิติการสมัคร:

```bash
# ดูการสมัครล่าสุด
./scripts/run-sql-via-api.sh scripts/check-rate-limit-stats.sql
```

### ตรวจสอบใน Admin Dashboard:

```
1. Login ด้วยบัญชี admin
2. ไปที่ /dashboard/admin/rate-limits
3. ดูสถิติ:
   - การสมัครในชั่วโมงที่แล้ว
   - การสมัครใน 24 ชั่วโมง
   - สถานะระบบ
```

---

## 🎯 สรุป

### สำหรับการทดสอบ Development:
✅ **ใช้ Admin API** (`node scripts/test-production-signup.js`)
- ไม่ถูก rate limit
- ทดสอบได้ทันที
- ครอบคลุมทุก flow

### สำหรับการทดสอบ UX:
✅ **ใช้ IP อื่น** (mobile hotspot/VPN)
- ทดสอบ UI จริงๆ
- ได้ user experience ที่แท้จริง

### สำหรับ Production จริง:
✅ **Rate limiting ทำงานปกติ**
- ป้องกัน spam
- ผู้ใช้ทั่วไปไม่น่าจะเจอปัญหา (ไม่ได้สมัครบ่อย)
- ถ้าเจอปัญหา: ให้ติดต่อ admin ใช้ฟีเจอร์ "สร้างผู้ใช้ใหม่"

---

## 📝 Next Steps

1. รันสคริปต์ทดสอบ: `node scripts/test-production-signup.js`
2. ตรวจสอบผลลัพธ์
3. ทดสอบ login ด้วยบัญชีที่สร้าง
4. ถ้าสำเร็จ = การสมัครทำงานปกติ ✅
5. ถ้าล้มเหลว = ดู Troubleshooting ด้านบน

