# คู่มือติดตามปัญหาผู้ใช้และตรวจสอบข้อมูล

## ภาพรวม

ระบบติดตามปัญหาช่วยให้คุณ:
- ✅ รู้ว่าผู้ใช้เจอปัญหาอะไรบ้าง
- ✅ ตรวจสอบว่าหลังสมัครแล้วข้อมูลถูกบันทึกครบถ้วน
- ✅ ระบุและแก้ไขปัญหาได้รวดเร็ว

---

## 🎯 วิธีตรวจสอบปัญหาผู้ใช้

### วิธีที่ 1: ใช้ Admin Dashboard (แนะนำ)

```
1. Login ด้วยบัญชี admin
2. ไปที่ /dashboard/admin/monitoring
3. ดูข้อมูล:
   - สถิติภาพรวม (การสมัคร, ข้อผิดพลาด)
   - ข้อผิดพลาดล่าสุด 20 รายการ
   - การสมัครที่ไม่สมบูรณ์
```

**ข้อมูลที่แสดง:**
- 📊 การสมัคร (7 วัน)
- 🔴 ข้อผิดพลาด (24 ชั่วโมง)
- 🟠 การสมัครไม่สมบูรณ์
- 🟡 Users ที่ไม่มี Profile

### วิธีที่ 2: ใช้สคริปต์ (รายละเอียดเต็ม)

```bash
cd sports-club-management

# รันสคริปต์ตรวจสอบทั้งหมด
./scripts/monitor-user-issues.sh
```

**ผลลัพธ์ที่ได้:**
1. ข้อผิดพลาดที่เกิดบ่อยใน 24 ชั่วโมง
2. Rate Limit Errors
3. การสมัครที่ไม่สมบูรณ์
4. Auth Users ที่ไม่มี Profile
5. Profiles ที่ไม่มี Athlete Record
6. สถิติการสมัครสมาชิก
7. ข้อผิดพลาดล่าสุด 10 รายการ

### วิธีที่ 3: ใช้ SQL โดยตรง

```bash
# ตรวจสอบปัญหา
./scripts/run-sql-via-api.sh scripts/check-user-issues.sql

# ตรวจสอบความสมบูรณ์ของข้อมูล
./scripts/run-sql-via-api.sh scripts/verify-registration-data.sql
```

---

## 📋 ตารางที่สร้างขึ้น

### 1. error_logs
เก็บ log ข้อผิดพลาดที่เกิดขึ้นในระบบ

**คอลัมน์:**
- `error_type`: ประเภท (auth, registration, rate_limit, validation, database, unknown)
- `error_code`: รหัส error
- `error_message`: ข้อความ error
- `error_details`: รายละเอียดเพิ่มเติม (JSON)
- `page_url`: หน้าที่เกิด error
- `user_id`: ผู้ใช้ที่เจอ error (ถ้ามี)
- `created_at`: เวลาที่เกิด

**ตัวอย่างการใช้งาน:**
```sql
-- ดู error ที่เกิดบ่อยใน 24 ชั่วโมง
SELECT error_type, error_code, COUNT(*) as count
FROM error_logs
WHERE created_at > NOW() - INTERVAL '24 hours'
GROUP BY error_type, error_code
ORDER BY count DESC;
```

### 2. registration_audit
ติดตามขั้นตอนการสมัครสมาชิก

**คอลัมน์:**
- `step`: ขั้นตอน (started, auth_created, profile_created, athlete_created, completed, failed)
- `email`: อีเมลผู้สมัคร
- `user_id`: User ID (ถ้ามี)
- `step_data`: ข้อมูลเพิ่มเติม (JSON)
- `error_message`: ข้อความ error (ถ้ามี)
- `created_at`: เวลา

**ตัวอย่างการใช้งาน:**
```sql
-- ดูว่าผู้ใช้คนนี้ทำถึงขั้นตอนไหน
SELECT step, created_at, error_message
FROM registration_audit
WHERE email = 'user@example.com'
ORDER BY created_at;
```

### 3. incomplete_registrations (View)
แสดงการสมัครที่ยังไม่เสร็จสมบูรณ์

**คอลัมน์:**
- `email`: อีเมล
- `last_attempt`: ครั้งสุดท้ายที่พยายาม
- `completed_steps`: ขั้นตอนที่ทำแล้ว
- `attempt_count`: จำนวนครั้งที่พยายาม
- `last_error`: error ล่าสุด

---

## 🔍 การตรวจสอบความสมบูรณ์ของข้อมูล

### ตรวจสอบว่าหลังสมัครแล้วข้อมูลครบหรือไม่

```bash
./scripts/run-sql-via-api.sh scripts/verify-registration-data.sql
```

**ผลลัพธ์ที่ได้:**

#### 1. การสมัครล่าสุด 20 รายการ
```
email                  | email_confirmed | has_profile | has_athlete | has_club
-----------------------|-----------------|-------------|-------------|----------
test@example.com       | ✅              | ✅          | ✅          | ✅
user@test.com          | ✅              | ✅          | ❌          | ❌
```

#### 2. สรุปความสมบูรณ์
```
total_signups | with_profile | with_athlete | profile_completion_rate
--------------|--------------|--------------|------------------------
50            | 48           | 45           | 96.00%
```

#### 3. ข้อมูลที่ขาดหาย
```
issue_type                  | count
----------------------------|-------
Missing Profile             | 2
Missing Athlete Record      | 3
Missing Club Assignment     | 1
Unconfirmed Email          | 5
```

---

## 🚨 ปัญหาที่พบบ่อยและวิธีแก้

### ปัญหา 1: Rate Limit Errors เยอะ

**สาเหตุ:**
- มีคนพยายามสมัครบ่อยเกินไป
- หรือมี bot พยายาม spam

**วิธีตรวจสอบ:**
```sql
SELECT COUNT(*), COUNT(DISTINCT ip_address)
FROM error_logs
WHERE error_type = 'rate_limit'
  AND created_at > NOW() - INTERVAL '24 hours';
```

**วิธีแก้:**
1. ถ้าเป็นผู้ใช้จริง: ใช้ Admin API สร้าง user ให้
2. ถ้าเป็น bot: ตรวจสอบ IP และพิจารณา block

### ปัญหา 2: Auth Users ไม่มี Profile

**สาเหตุ:**
- Database trigger ไม่ทำงาน
- RLS policy บล็อก
- Error ระหว่างสร้าง profile

**วิธีตรวจสอบ:**
```sql
SELECT au.id, au.email, au.created_at
FROM auth.users au
LEFT JOIN profiles p ON p.id = au.id
WHERE p.id IS NULL
  AND au.created_at > NOW() - INTERVAL '7 days';
```

**วิธีแก้:**
```bash
# สร้าง profile ให้ users ที่ขาด
./scripts/run-sql-via-api.sh scripts/create-profiles-for-users.sql
```

### ปัญหา 3: Profiles ไม่มี Athlete Record

**สาเหตุ:**
- ไม่มี club ในระบบ
- Error ระหว่างสร้าง athlete record

**วิธีตรวจสอบ:**
```sql
SELECT p.id, p.email, p.full_name
FROM profiles p
LEFT JOIN athletes a ON a.user_id = p.id
WHERE a.user_id IS NULL
  AND p.role = 'athlete'
  AND p.created_at > NOW() - INTERVAL '7 days';
```

**วิธีแก้:**
```sql
-- สร้าง athlete record (ต้องมี club_id)
INSERT INTO athletes (user_id, club_id, date_of_birth, phone_number)
SELECT 
  p.id,
  (SELECT id FROM clubs LIMIT 1), -- ใช้ club แรก
  '2000-01-01',
  '0000000000'
FROM profiles p
LEFT JOIN athletes a ON a.user_id = p.id
WHERE a.user_id IS NULL
  AND p.role = 'athlete';
```

### ปัญหา 4: การสมัครไม่สมบูรณ์

**สาเหตุ:**
- ผู้ใช้ปิดหน้าต่างกลางคัน
- เกิด error ระหว่างทาง
- Network timeout

**วิธีตรวจสอบ:**
```sql
SELECT * FROM incomplete_registrations;
```

**วิธีแก้:**
1. ติดต่อผู้ใช้เพื่อให้ลองใหม่
2. หรือใช้ Admin API สร้าง user ให้เลย

---

## 📊 Dashboard Metrics

### KPIs ที่ควรติดตาม:

#### 1. Profile Completion Rate
```
เป้าหมาย: > 95%
คำนวณ: (users with profile / total users) * 100
```

#### 2. Athlete Record Completion Rate
```
เป้าหมาย: > 90%
คำนวณ: (profiles with athlete / total athlete profiles) * 100
```

#### 3. Error Rate
```
เป้าหมาย: < 5%
คำนวณ: (errors / total signups) * 100
```

#### 4. Rate Limit Hit Rate
```
เป้าหมาย: < 2%
คำนวณ: (rate limit errors / total signup attempts) * 100
```

---

## 🔧 Maintenance Tasks

### รายวัน:
```bash
# ตรวจสอบ error ที่เกิดใน 24 ชั่วโมง
./scripts/run-sql-via-api.sh scripts/check-user-issues.sql
```

### รายสัปดาห์:
```bash
# ตรวจสอบความสมบูรณ์ของข้อมูล
./scripts/run-sql-via-api.sh scripts/verify-registration-data.sql

# แก้ไขข้อมูลที่ขาดหาย
./scripts/run-sql-via-api.sh scripts/create-profiles-for-users.sql
```

### รายเดือน:
```sql
-- ลบ error logs เก่า (เก็บแค่ 30 วัน)
DELETE FROM error_logs
WHERE created_at < NOW() - INTERVAL '30 days';

-- ลบ registration audit เก่า (เก็บแค่ 30 วัน)
DELETE FROM registration_audit
WHERE created_at < NOW() - INTERVAL '30 days';
```

---

## 📝 สรุป

### ไฟล์ที่สร้างขึ้น:

**Database:**
- `scripts/60-create-error-logs-table.sql` - ตาราง error_logs
- `scripts/61-create-registration-audit-table.sql` - ตาราง registration_audit

**Scripts:**
- `scripts/check-user-issues.sql` - ตรวจสอบปัญหา
- `scripts/verify-registration-data.sql` - ตรวจสอบความสมบูรณ์
- `scripts/monitor-user-issues.sh` - รันทั้งหมดพร้อมกัน

**Code:**
- `lib/monitoring/error-logger.ts` - Functions สำหรับ log errors
- `app/dashboard/admin/monitoring/page.tsx` - Admin dashboard

### วิธีใช้งานเบื้องต้น:

```bash
cd sports-club-management

# 1. ดูปัญหาและข้อมูลทั้งหมด
./scripts/monitor-user-issues.sh

# 2. หรือเปิด Admin Dashboard
# ไปที่ /dashboard/admin/monitoring
```

### Next Steps:

1. ✅ ตรวจสอบว่ามี error อะไรบ้าง
2. ✅ แก้ไขข้อมูลที่ขาดหาย
3. ✅ ติดตาม metrics เป็นประจำ
4. ✅ ปรับปรุงระบบตามปัญหาที่พบ

